include LICENCE RELEASE-NOTES AUTHORS
include README.md
include bedrock.desktop
include *.py
include run_bedrock
include org.bedrock.bedrock.metainfo.xml
recursive-include packages *.py
recursive-include packages cacert.pem

include contrib/requirements/requirements*.txt
include contrib/deterministic-build/requirements*.txt
include contrib/*.sh

graft bedrock
graft tests
graft contrib/udev

exclude bedrock/*.so
exclude bedrock/*.so.0
exclude bedrock/*.dll
exclude bedrock/*.dylib

global-exclude __pycache__
global-exclude *.py[co~]
global-exclude *.py.orig
global-exclude *.py.rej
global-exclude .git

# We include both source (.po) and compiled (.mo) locale files (if present).
# When building the "sourceonly" tar.gz, the build script explicitly deletes the compiled files.
# exclude bedrock/locale/locale/*/LC_MESSAGES/bedrock.mo
